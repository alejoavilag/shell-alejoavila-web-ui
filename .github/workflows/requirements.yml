name: 📦 Requirements

on:
  pull_request:
    branches:
      - master

jobs:
    check-files:
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: '.'
        steps:
        - uses: actions/checkout@v3
        - name: Setup Python
          uses: actions/setup-python@v3
          with:
            python-version: '3.12'
        - name: Execute validation
          run: |
            echo "Checking for requirements.yml file..."
